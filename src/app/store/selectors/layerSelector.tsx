// import { createSelector } from 'reselect';
// import { ApplicationState } from 'app/store';
// import { LayerType } from 'app/constants/Type';

// export const getAllLayersSelector = (state: ApplicationState) => state.layer.layer || ({} as LayerType);

// export const getLoadingLayersSelector = (state: ApplicationState) => state.layer.loading || false;
